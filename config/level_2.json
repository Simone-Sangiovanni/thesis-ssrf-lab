{
  "_comment": [
    "Level 2 — file:// blocked.",
    "The student must send an HTTP request to the internal server",
    "(e.g. http://127.0.0.1/) to retrieve the flag.",
    "No host restrictions yet — localhost is freely reachable."
  ],
  "pipeline": [
    "checkProtocol"
  ],
  "allowedProtocols": ["http", "https"],
  "hostBlacklist": [],
  "fileWhitelist": ["/flags/level_2"],
  "randomPort": false
}